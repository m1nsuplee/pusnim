import { createElement, createRoot } from './core';
import { useEffect, useState } from './hooks';

export { useState, useEffect };

const Pusnim = {
  createRoot,
  createElement,
};

export default Pusnim;
